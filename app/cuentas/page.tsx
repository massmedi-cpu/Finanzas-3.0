import { formatEuro, formatInteger } from "@/lib/format/es-es";
import Link from "next/link";import { requireAuthorizedUser } from "@/lib/auth/require-user";import { getAccountsOverview } from "@/lib/financial/accounts";import { BalanceChart } from "@/components/balance-chart";const date=new Intl.DateTimeFormat("es-ES",{day:"2-digit",month:"2-digit",year:"numeric"});export const dynamic="force-dynamic";export default async function AccountsPage(){await requireAuthorizedUser();const data=await getAccountsOverview();return <main className="app-shell"><section id="main-content" tabIndex={-1} className="workspace accounts-workspace"><header className="topbar"><div><p className="eyebrow">CUENTAS · {data.version}</p><h1>Cuentas</h1><p>Saldos actuales, actividad y evolución de cada producto financiero.</p></div><Link className="ghost button-link" href="/">Volver al inicio</Link></header><section className="accounts-total" aria-label="Patrimonio financiero disponible"><div><span>Total disponible</span><strong>{formatEuro(data.totalAvailable)}</strong></div><p>{data.accounts.length} cuentas activas · saldos obtenidos del último movimiento con saldo de cada cuenta real.</p></section><div className="accounts-list">{data.accounts.map(account=><article className={`account-overview ${account.role==="operating"?"operating":"savings"}`} key={account.id}><div className="account-overview-head"><div><p className="eyebrow">{account.role==="operating"?"OPERATIVA":"AHORRO"}</p><h2>{account.name}</h2><span>{account.institution||"Entidad no indicada"} · {account.identifier}</span></div><div className="account-balance"><small>Saldo actual</small><strong>{account.balance==null?"—":formatEuro(account.balance)}</strong><span>{account.balanceDate?`Actualizado ${date.format(new Date(account.balanceDate+"T12:00:00"))}`:"Sin fecha de saldo"}</span></div></div><BalanceChart points={account.balanceSeries} compact/><div className="account-stats"><div><span>Ingresos del mes</span><strong>{formatEuro(account.monthIncome)}</strong></div><div><span>Gastos del mes</span><strong>{formatEuro(account.monthExpenses)}</strong></div><div><span>Neto del mes</span><strong className={account.monthNet<0?"negative":"positive"}>{formatEuro(account.monthNet)}</strong></div><div><span>Movimientos</span><strong>{formatInteger(account.movements)}</strong></div></div><div className="account-sources">{account.sources.map(source=><span key={source.identifier} className={source.primary?"source-primary":"source-linked"}>{source.primary?"Cuenta":"Vinculada"}: {source.identifier}</span>)}{!account.cashFlowEnabled&&<span className="source-rule">Excluida del Cash Flow</span>}</div><div className="account-actions"><Link href={`/cuentas/${account.id}`}>Ver detalle de la cuenta →</Link><Link href={`/movimientos?account=${encodeURIComponent(account.id)}`}>Ver movimientos →</Link></div></article>)}</div></section></main>}
