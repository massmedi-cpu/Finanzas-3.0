import { NextRequest,NextResponse } from "next/server";
import { getAuthorizedClient } from "@/lib/auth/authorized-client";
import type { MovementsResponse } from "@/lib/financial/movements";
function numberParam(value:string|null){if(!value)return null;const number=Number(value.replace(",","."));return Number.isFinite(number)?number:null}
function booleanParam(value:string|null):boolean|null{if(value==null||value==="")return null;const normalized=value.toLowerCase();if(["1","true","yes","si","sí"].includes(normalized))return true;if(["0","false","no"].includes(normalized))return false;return null}
const noStore={"Cache-Control":"private, no-store"};export const dynamic="force-dynamic";
export async function GET(request:NextRequest){const supabase=await getAuthorizedClient();if(!supabase)return NextResponse.json({ok:false,error:"unauthorized"},{status:401});const q=request.nextUrl.searchParams;const{data,error}=await supabase.rpc("financial_app_movements_advanced_v14",{p_page:Math.max(1,Number(q.get("page")||1)),p_page_size:Math.min(200,Math.max(1,Number(q.get("pageSize")||50))),p_search:q.get("search")||null,p_account_id:q.get("account")||null,p_type:q.get("type")||null,p_category:q.get("category")||null,p_subcategory:q.get("subcategory")||null,p_channel:q.get("channel")||null,p_tag:q.get("tag")||null,p_review_only:q.get("review")==="1",p_recurring:booleanParam(q.get("recurring")),p_internal_transfer:booleanParam(q.get("internalTransfer")),p_reconciled:booleanParam(q.get("reconciled")),p_has_documents:booleanParam(q.get("documents")),p_has_splits:booleanParam(q.get("splits")),p_date_from:q.get("from")||null,p_date_to:q.get("to")||null,p_min_amount:numberParam(q.get("min")),p_max_amount:numberParam(q.get("max")),p_sort:q.get("sort")||"date_desc",p_merchant:q.get("merchant")||null,p_cash_flow_only:booleanParam(q.get("cashFlow"))===true,p_duplicate:booleanParam(q.get("duplicate"))});if(error||!data)return NextResponse.json({ok:false,error:error?.message||"movements_unavailable"},{status:400});return NextResponse.json(data as MovementsResponse,{headers:noStore})}
export async function POST(request:NextRequest){const supabase=await getAuthorizedClient();if(!supabase)return NextResponse.json({ok:false,error:"unauthorized"},{status:401});const body=await request.json().catch(()=>null);if(body?.kind!=="seen"||!Array.isArray(body.ids))return NextResponse.json({ok:false,error:"invalid_action"},{status:400});const ids=[...new Set(body.ids.filter((id:unknown)=>typeof id==="string"&&/^[0-9a-f-]{36}$/i.test(id)).slice(0,200))];if(!ids.length)return NextResponse.json({ok:true,updated:0},{headers:noStore});const{data,error}=await supabase.rpc("financial_app_mark_new_seen",{p_ids:ids});if(error)return NextResponse.json({ok:false,error:error.message},{status:400});return NextResponse.json({ok:true,updated:Number(data||0)},{headers:noStore})}
