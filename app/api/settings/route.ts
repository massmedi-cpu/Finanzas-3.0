import { NextRequest,NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { hasFinancialAppAccess,normalizeEmail } from "@/lib/auth/access";
async function authorizedClient(){const supabase=await createClient();const {data,error}=await supabase.auth.getUser();const email=normalizeEmail(data.user?.email);if(error||!data.user||!(await hasFinancialAppAccess(supabase,email)))return null;return supabase}
export async function GET(){const supabase=await authorizedClient();if(!supabase)return NextResponse.json({ok:false,error:"unauthorized"},{status:401});const {data,error}=await supabase.rpc("financial_app_settings_overview");if(error||!data)return NextResponse.json({ok:false,error:error?.message||"settings_unavailable"},{status:400});return NextResponse.json({ok:true,data},{headers:{"Cache-Control":"private, no-store"}})}
export async function PATCH(request:NextRequest){const supabase=await authorizedClient();if(!supabase)return NextResponse.json({ok:false,error:"unauthorized"},{status:401});const body=await request.json().catch(()=>null);const theme=String(body?.theme||"system");const timezone=String(body?.timezone||"Europe/Madrid");const {data,error}=await supabase.rpc("financial_app_settings_update",{p_theme:theme,p_timezone:timezone});if(error||!data)return NextResponse.json({ok:false,error:error?.message||"settings_update_failed"},{status:400});return NextResponse.json({ok:true,data},{headers:{"Cache-Control":"private, no-store"}})}
