import { NextRequest,NextResponse } from "next/server";
import { getAuthorizedClient } from "@/lib/auth/authorized-client";
const noStore={"Cache-Control":"private, no-store"};export const dynamic="force-dynamic";
export async function GET(){const supabase=await getAuthorizedClient();if(!supabase)return NextResponse.json({ok:false,error:"unauthorized"},{status:401});const{data,error}=await supabase.rpc("financial_app_backup_export");if(error||!data)return NextResponse.json({ok:false,error:error?.message||"backup_unavailable"},{status:400});const day=new Date().toISOString().slice(0,10);return new NextResponse(JSON.stringify(data,null,2),{status:200,headers:{...noStore,"Content-Type":"application/json; charset=utf-8","Content-Disposition":`attachment; filename="financial-app-private-backup-${day}.json"`}})}
export async function POST(request:NextRequest){const supabase=await getAuthorizedClient();if(!supabase)return NextResponse.json({ok:false,error:"unauthorized"},{status:401});const length=Number(request.headers.get("content-length")||0);if(length>10*1024*1024)return NextResponse.json({ok:false,error:"backup_too_large"},{status:413});const backup=await request.json().catch(()=>null);if(!backup||typeof backup!=="object")return NextResponse.json({ok:false,error:"invalid_json"},{status:400});const{data,error}=await supabase.rpc("financial_app_backup_validate",{p_backup:backup});if(error||!data)return NextResponse.json({ok:false,error:error?.message||"backup_validation_failed"},{status:400});return NextResponse.json(data,{headers:noStore})}
