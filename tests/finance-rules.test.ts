import { describe, expect, it } from "vitest";
import { calculateCashFlow, isCashFlowComputable } from "@/lib/finance/rules";
import type { FinancialMovement } from "@/lib/finance/types";
const movement=(overrides:Partial<FinancialMovement>={}):FinancialMovement=>({sourceId:"CC-1",accountKind:"current",amountCents:-1000,movementType:"Gasto",...overrides});
describe("Axioma de Cash Flow",()=>{it("excluye absolutamente todos los movimientos de cuenta ahorro",()=>{expect(isCashFlowComputable(movement({accountKind:"savings",amountCents:19050,movementType:"Ingreso"}))).toBe(false);expect(isCashFlowComputable(movement({accountKind:"savings",amountCents:-50000,movementType:"Gasto"}))).toBe(false)});it("excluye traspasos, duplicados y exclusiones manuales",()=>{expect(isCashFlowComputable(movement({isOwnAccountTransfer:true}))).toBe(false);expect(isCashFlowComputable(movement({isDuplicate:true}))).toBe(false);expect(isCashFlowComputable(movement({excludedFromCashFlow:true}))).toBe(false)});it("calcula ingresos, gastos y neto solo con movimientos computables",()=>{const result=calculateCashFlow([movement({sourceId:"I1",amountCents:200000,movementType:"Ingreso"}),movement({sourceId:"G1",amountCents:-50000}),movement({sourceId:"AH1",accountKind:"savings",amountCents:19050}),movement({sourceId:"T1",amountCents:-10000,isOwnAccountTransfer:true})]);expect(result).toEqual({incomeCents:200000,expenseCents:50000,netCents:150000,includedSourceIds:["I1","G1"]})})});
