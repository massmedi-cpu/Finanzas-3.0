# Financial App 10.0.12 — reintento de despliegue a Production

Este commit es únicamente documental y no modifica código de producto.

Financial App 10.0.12 ya está integrada en `main` mediante `02de3eb8e196af2eebaa5626045a7678fe9a9421`. Este commit fuerza un nuevo intento de despliegue a Production manteniendo idéntico el código funcional de 10.0.12.

Versión canónica en `package.json`: `10.0.12`.
Candidato funcional validado: `9adab17c7ca4b022fbd331b4244ea4da104aea15`.
Preview validada disponible: `dpl_8AgSyWWemGRNM4BwoHUzU7Fw5ydE` (mismo código de producto; el HEAD final sólo añade una espera de hidratación en prueba).