name: Analysis Premium Targeted E2E

on:
  push:
    branches:
      - 'commercial-readiness/analysis-premium-v2'

permissions:
  contents: read

jobs:
  analysis-targeted-e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Checkout exact branch commit
        uses: actions/checkout@v6

      - name: Setup Node.js 24
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci --ignore-scripts --no-audit --no-fund

      - name: Build production bundle
        run: npm run build

      - name: Install Chromium
        shell: bash
        run: |
          set -euo pipefail
          for source in /etc/apt/sources.list.d/*; do
            if sudo grep -q 'dl.google.com/linux/chrome' "$source" 2>/dev/null; then
              sudo mv "$source" "${source}.disabled"
            fi
          done
          npx playwright install --with-deps chromium

      - name: Run Analysis E2E only
        run: >-
          npm run test:e2e --
          tests/e2e/analysis.spec.ts
          --project=chromium-desktop
          --project=chromium-mobile

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: analysis-premium-targeted-report-${{ github.sha }}
          path: playwright-report/
          retention-days: 7

  analysis-protected-preview:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    permissions:
      contents: read
      deployments: read
      id-token: write
    env:
      VERCEL_AUTOMATION_BYPASS_SECRET: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}
      VERIFIED_FALLBACK_PREVIEW_SHA: 2371d49fae6923ddcd2d82b38fc1449543d9ab85
      VERIFIED_FALLBACK_PREVIEW_URL: https://finanzas-3-0-lub42w652-massmedi-9832s-projects.vercel.app
    steps:
      - name: Generate ephemeral GitHub OIDC token
        id: oidc
        uses: actions/github-script@v8
        with:
          script: |
            const token = await core.getIDToken();
            core.setSecret(token);
            core.setOutput('token', token);

      - name: Discover exact Preview or select verified fallback
        id: vercel
        uses: actions/github-script@v8
        env:
          FALLBACK_SHA: ${{ env.VERIFIED_FALLBACK_PREVIEW_SHA }}
          FALLBACK_URL: ${{ env.VERIFIED_FALLBACK_PREVIEW_URL }}
        with:
          script: |
            const [owner, repo] = process.env.GITHUB_REPOSITORY.split('/');
            const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));

            async function successfulPreview(deployment) {
              const statuses = await github.rest.repos.listDeploymentStatuses({
                owner,
                repo,
                deployment_id: deployment.id,
                per_page: 30,
              });
              const status = statuses.data.find((candidate) => {
                const url = candidate.environment_url || candidate.target_url || '';
                return candidate.state === 'success' && /\.vercel\.app\/?$/i.test(url);
              });
              if (!status) return null;
              return (status.environment_url || status.target_url).replace(/\/$/, '');
            }

            for (let attempt = 1; attempt <= 4; attempt += 1) {
              const exact = await github.rest.repos.listDeployments({ owner, repo, sha: context.sha, per_page: 30 });
              for (const deployment of exact.data) {
                const url = await successfulPreview(deployment);
                if (url) {
                  core.setOutput('url', url);
                  core.setOutput('sha', deployment.sha);
                  core.setOutput('exact', 'true');
                  core.info(`Exact Preview discovered on attempt ${attempt}: ${url}`);
                  return;
                }
              }
              await sleep(5000);
            }

            const fallbackSha = process.env.FALLBACK_SHA;
            const fallbackUrl = process.env.FALLBACK_URL;
            if (!fallbackSha || !fallbackUrl) {
              core.setFailed('Verified fallback Preview metadata is missing.');
              return;
            }
            core.setOutput('url', fallbackUrl.replace(/\/$/, ''));
            core.setOutput('sha', fallbackSha);
            core.setOutput('exact', 'false');
            core.warning(`Vercel exact Preview unavailable; using verified fallback ${fallbackSha}: ${fallbackUrl}`);

      - name: Verify fallback cannot hide Analysis API changes
        if: steps.vercel.outputs.exact != 'true'
        uses: actions/github-script@v8
        env:
          PREVIEW_SHA: ${{ steps.vercel.outputs.sha }}
        with:
          script: |
            const [owner, repo] = process.env.GITHUB_REPOSITORY.split('/');
            const previewSha = process.env.PREVIEW_SHA;
            if (!previewSha) {
              core.setFailed('Fallback Preview SHA is missing.');
              return;
            }
            const comparison = await github.rest.repos.compareCommitsWithBasehead({
              owner,
              repo,
              basehead: `${previewSha}...${context.sha}`,
              per_page: 100,
            });
            if (!['ahead', 'identical'].includes(comparison.data.status)) {
              core.setFailed(`Fallback Preview ${previewSha} is not an ancestor of ${context.sha}.`);
              return;
            }
            const criticalPrefixes = [
              'app/api/analysis/',
              'src/application/analysis/analysis-engine.ts',
              'src/application/analysis/analysis-loader.ts',
              'src/infrastructure/persistence/vercel-supabase-gateway.ts',
              'supabase/functions/financial-app-db-gateway/analysis-query.ts',
              'supabase/functions/financial-app-db-gateway/financial-logic.ts',
              'supabase/functions/financial-app-db-gateway/index.ts',
              'package.json',
              'package-lock.json',
              'next.config',
            ];
            const unsafe = (comparison.data.files || [])
              .map((file) => file.filename)
              .filter((filename) => criticalPrefixes.some((prefix) => filename === prefix || filename.startsWith(prefix)));
            if (unsafe.length > 0) {
              core.setFailed(`Fallback Preview is unsafe because Analysis API/runtime sources changed: ${unsafe.join(', ')}`);
              return;
            }
            core.info(`Fallback is safe: no Analysis API/runtime source changed between ${previewSha} and ${context.sha}.`);

      - name: Checkout exact branch commit
        uses: actions/checkout@v6

      - name: Setup Node.js 24
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci --ignore-scripts --no-audit --no-fund

      - name: Install Chromium
        shell: bash
        run: |
          set -euo pipefail
          for source in /etc/apt/sources.list.d/*; do
            if sudo grep -q 'dl.google.com/linux/chrome' "$source" 2>/dev/null; then
              sudo mv "$source" "${source}.disabled"
            fi
          done
          npx playwright install --with-deps chromium

      - name: Run Analysis against protected Preview
        env:
          VERCEL_PREVIEW_URL: ${{ steps.vercel.outputs.url }}
          VERCEL_TRUSTED_OIDC_TOKEN: ${{ steps.oidc.outputs.token }}
        run: >-
          npm run test:e2e --
          tests/e2e/analysis.spec.ts
          --project=chromium-desktop

      - name: Upload protected Preview report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: analysis-premium-protected-preview-report-${{ github.sha }}
          path: playwright-report/
          retention-days: 7
